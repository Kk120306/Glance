@AGENTS.md

## Claude Code

- Invoke skills: `spec`, `plan`, `implement`, `tdd`, `debug`, `review`, `task-to-pr`, `browser-verify`, `commit`, `pr`
- `browser-verify` requires Chrome DevTools MCP
- Agent definitions live in `.claude/agents/`
- Keep shared repo guidance in `AGENTS.md`; Claude-specific adapter notes here
- Read `CONTEXT.md` for domain vocabulary before writing patient-side or camera-related code
